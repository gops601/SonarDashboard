name: SonarQube Scan

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:
    inputs:
      student_repo:
        description: 'Student Repository (e.g. org/repo)'
        required: false
        type: string

jobs:
  sonar:
    runs-on: ubuntu-latest

    steps:

      # ---------------------------
      # Checkout Code
      # ---------------------------
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.student_repo || github.repository }}

      # ---------------------------
      # Setup Java (Required)
      # ---------------------------
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # ---------------------------
      # Setup Python
      # ---------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # ---------------------------
      # Install Dependencies
      # ---------------------------
      - name: Install dependencies
        run: |
          pip install -r requirements.txt || true
          pip install pytest pytest-cov

      # ---------------------------
      # Fix Import Path
      # ---------------------------
      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$PWD" >> $GITHUB_ENV

      # ---------------------------
      # Run Tests with Coverage
      # ---------------------------
      - name: Run tests
        run: |
          if [ -d "tests" ]; then
            pytest tests/ --cov=. --cov-report=xml || true
          else
            echo "No tests directory found. Creating empty coverage."
            echo "<?xml version='1.0' ?><coverage version='7.6.1'></coverage>" > coverage.xml
          fi

      # ---------------------------
      # Verify Coverage File
      # ---------------------------
      - name: Check coverage
        run: |
          ls -l coverage.xml || true

      # ---------------------------
      # Determine Project Key
      # ---------------------------
      - name: Determine Project Key
        run: |
          if [ -n "${{ inputs.student_repo }}" ]; then
            REPO_CLEAN=$(echo "${{ inputs.student_repo }}" | tr '/' '-')
            echo "PROJECT_KEY=${{ github.repository_owner }}_${REPO_CLEAN}_${{ github.actor }}_${{ github.repository_id }}" >> $GITHUB_ENV
          else
            echo "PROJECT_KEY=${{ github.repository_owner }}_${{ github.event.repository.name }}_${{ github.actor }}_${{ github.repository_id }}" >> $GITHUB_ENV
          fi

      # ---------------------------
      # SonarQube Scan
      # ---------------------------
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_HOST_URL: http://187.127.142.34:9000
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=${{ env.PROJECT_KEY }}
            -Dsonar.projectName=${{ github.event.repository.name }}-${{ github.actor }}
            -Dsonar.sources=.
            -Dsonar.python.coverage.reportPaths=coverage.xml